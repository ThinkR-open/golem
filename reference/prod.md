# Is the app in dev mode or prod mode?

Is the app in dev mode or prod mode?

## Usage

``` r
app_prod()

app_dev()
```

## Value

`TRUE` or `FALSE` depending on the status of
`getOption( "golem.app.prod")`
